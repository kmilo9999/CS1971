It doesnt resize the game elements on the screen. 
It zooms but it doesnt clip elements in the scaled viewport.
Polygons Colliders are returning mtv and axis in the same direction. Which causes the collision to continue happening indefinitely
