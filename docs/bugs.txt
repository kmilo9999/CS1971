It doesnt resize the game elements on the screen. 
It zooms but it doesnt clip elements in the scaled viewport.
Sometimes after loading the level from the file, the main charater cant be moved during a few seconds. After that it works
